# Development overrides — auto-loaded by docker compose
# In production, use: docker compose -f docker-compose.yml up -d
services:
  app:
    volumes:
      - .:/var/www

  nginx:
    volumes:
      - .:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro

  horizon:
    volumes:
      - .:/var/www

  scheduler:
    volumes:
      - .:/var/www

  migrate:
    volumes:
      - .:/var/www
